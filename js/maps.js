function openMaps() {
    // var daddr = window.location.search.slice(1);
    // if (navigator.geolocation) {
    //   navigator.geolocation.getCurrentPosition(function(data){
    // 	if (data.coords) {
          window.location = 'https://maps.google.fr/maps?q=21.188154,79.660700'
    // 	}
    //   });
    // }
}